# .ROOT project-level Codex sandbox/approval config. See 00-BRAIN\CODEX.md
# for governance. The canonical workspace is this local C: vault; G: is backup
# only. Keep approval_policy as the human-in-the-loop backstop for consequential
# actions even though local unelevated sandbox operation is now working.

sandbox_mode = "workspace-write"
approval_policy = "on-request"

[sandbox_workspace_write]
network_access = false
