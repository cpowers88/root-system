---
type: flags
timeline: now
status: active
tags: [governance]
---

# SYSTEM_FLAGS.md — Open Improvement Flags
### Location: 00-BRAIN\ | Check at every session start.
### Last updated: August 11, 2026, flag #95 opened (config declares controls that do not apply in the environment reading it)

---

## The Rule

Every system improvement flag lands here the moment it is raised — in a session, a handoff, a weekly, anywhere.

**Timing by priority:**
- **HIGH** — fix in the session that raised it. Do not close the session with an open HIGH flag.
- **MEDIUM** — fix at the next weekly review.
- **LOW** — fix at the next monthly review.

A flag leaves this file only when the fix is verified in the target file. "I'll remember" is not a status.

If the same flag is re-raised after being closed, it comes back as HIGH.

**History rule (updated July 15, 2026):** this file holds OPEN flags only. When
a flag closes, its row moves in the same session to the monthly ledger at
`00-BRAIN\Session_Logs\Closed Flags\CLOSED_FLAGS_YYYY-MM.md` — no weekly
migration step to forget. Pre-ledger history (June 8 – July 11, 83 rows):
`99-ARCHIVE\ARCHIVED_2026-07-11_SYSTEM_FLAGS_CLOSED_TABLE.md`. This file is
read at every session start — history in it is a per-session context tax.

---

## OPEN FLAGS

| # | Flag | Raised | Priority | Target | Status |
|---|---|---|---|---|---|
| 95 | **Configuration declares controls that silently do not apply in the environment reading it.** Not three unrelated bugs — one structural gap: a single config set is read from two environments with no per-environment resolution, so a rule can be syntactically valid, pass its gate, and protect nothing. Three measured instances in two days: (1) `user-settings-policy.template.json`'s five vault denies are written `~/.ROOT/...`, which in WSL expands to `/home/theinternet/.ROOT` — a path that does not exist; deploying unchanged would have installed five rules guarding an empty directory while the real vault stayed open (caught before deploy 2026-08-11, and `validate_boot_chain.py` is now environment-aware). (2) `.claude/settings.json`'s `sandbox` block declares `enabled: true`, a `denyWrite` list, and a two-domain `allowedDomains` allowlist, all of which are **inert** — measured across four independent dimensions: no `bwrap` ancestor in process lineage, `Seccomp: 0`/`NoNewPrivs: 0`, no sandbox-attributable namespace isolation, and egress to `example.com` returning HTTP 200. (3) The project `permissions.allow` entries invoke `python`, which does not exist in WSL (only `python3`), so those allow rules never match there and every scripted run prompts. The common failure mode is the dangerous one: **a control that reads as protection in an audit while enforcing nothing.** The `~/.ROOT` case would have gone green in the validator. | August 11 | MEDIUM | Audit every environment-dependent value in `.claude/settings.json`, `settings.local.json`, and `user-settings-policy.template.json` for per-environment resolution, and give each declared control a test that proves enforcement rather than presence. Do **not** delete the `sandbox` block — keep it so it activates if native support lands — but record beside it that it is currently unenforced, so no future audit reads it as a live control. Interpreter references should resolve per environment (`python` vs `python3`) rather than assume Windows. | OPEN — pattern identified and evidenced; instance (1) fixed, (2) and (3) documented and unfixed |
| 57 | **EDUCATION syllabus data-quality gaps.** D2L is accessible but PHYS 2211 Section 54 and ENGR 1000 Fall BWD remain unpopulated. Exact-section CSE 1321, CSE 1321L, ECON 1000, and TCOM 2010 captures are all filed and current (see `02-LIBRARY\00-school\SYLLABUS_STATUS.md`). Neighboring PHYS Sections 51 and 55 remain reference-only and cannot establish Section 54 grading, dates, or policy. **2026-07-29:** `02-LIBRARY\00-school\fall_KSU_schedule.md` (Chris's actual Outlook registration confirmation) independently confirms **Farhan Islam** and PHYS 2211 §54's exact meeting times/CRN 83722 — stronger than the prior provisional online-listing match, but still not the syllabus content (grading, exams, policy, calendar). A dated punch list with an Aug 17 escalation trigger for both remaining gaps now lives in `SYLLABUS_STATUS.md`'s Pre-Semester Punch List. | July 9; updated July 29 | MEDIUM | Recheck D2L/Simple Syllabus weekly from mid-August; if nothing posts by **Aug 17**, email the instructor directly. Full detail and owner in `SYLLABUS_STATUS.md`. | OPEN — four of six courses fully filed and current; PHYS §54 instructor confirmed via two independent sources, syllabus content still missing; ENGR BWD source still missing |
| 16 | Spin rule / right-hand rule needs physical anchor from Atlas. Covers: cross product, torque, angular velocity, and future magnetic field direction. Curl fingers in direction of rotation, thumb points to vector. Must be anchored before these topics appear in PHYS 2211. | June 9 | LOW | Atlas / Physics sessions | OPEN — **approaching**: Chris is now working Vectors (Serway Ch 3) per castle current-position (July 8); cross product is next door. Atlas should anchor it in the next physics session that touches vector products. |
| 69 | `Agents SDK  OpenAI API 1.md` in `03-WIKIS\AI_AUTOMATION_SYSTEMS\raw\OPEN_AI-CHATGPT_CODEX_FILES\` is byte-identical (SHA-256 `0ddb73d5...92db1`) to `Agents SDK  OpenAI API.md` — same defect class as closed flag #63 (mis-saved duplicate). Content read once, not double-summarized. **2026-08-02:** Chris reviewed the full 85-line content (OpenAI Agents SDK docs clipping) and decided: archive the duplicate. Blocked on execution — `03-WIKIS\AI_AUTOMATION_SYSTEMS\raw\` is under a sandbox-level write guard that no available tool can pass, even with Chris's explicit approval in-session; this is a structural boundary, not a missing permission click. | July 12; decided Aug 2 | LOW | Chris to move `Agents SDK  OpenAI API 1.md` to `99-ARCHIVE\ARCHIVED_2026-08-02_AI_AUTOMATION_SYSTEMS_raw_Agents-SDK-OpenAI-API-duplicate.md` himself, outside the sandbox guard | OPEN — decision made, execution blocked by raw-folder sandbox guard, needs Chris to run the move directly |
| 93 | **HIGH-flag-before-close rule is prose, not enforced.** `.claude\skills\session-close\SKILL.md` (lines 38-39) states a HIGH flag must be fixed or explicitly handed to Chris before session close — but nothing currently checks this; a session can skip it. Full analysis and proposed target (`.claude\hooks\` or equivalent, firing on session-close/stop) in `03-WIKIS\AI_AUTOMATION_SYSTEMS\wiki\system-evolution\proposals\2026-07-12_session-close-high-flag-hook.md`. Chris approved moving forward 2026-08-07. | July 12; approved Aug 7 | MEDIUM | Codex to design the actual hook mechanics (trigger event, block vs. warn) per the proposal's Risk/Blast Radius note, then Claude Code or Codex implements. | OPEN — approved, needs Codex hook-mechanics design before implementation |
| 94 | **Teaching-hat methods load conditionally, so teaching quality varies by session.** `HAT_EDUCATOR.md` (line 50) names seven methods — Skeleton First, One Concept at a Time, Term Anchoring, Explain-It-Back, Cold Checks, Physical Anchors, Short Corrections — but their substance sits in `HAT_EDUCATOR_PLAYBOOKS.md` behind a judgment-call load ("Load when running a teaching session"). None of the five subject hats (`HAT_PHYSICS`, `HAT_PYTHON`, `HAT_TCOM`, `HAT_ECON`, `HAT_ENGR1000`) reference the playbooks, so after the educator hat nothing in the chain mentions the methods again. When the playbook loads, teaching follows the method; when it doesn't, the agent has seven bare names and improvises — the same hat behaving as two different teachers. Cause is the July 11, 2026 slim pass, which moved substance behind a conditional load to save ~300 words. Chris reports the hats are "iffy sometimes" and has re-tested them; this had never been recorded as a flag, which is why he could not tell whether it had been acted on. | August 10 | MEDIUM | Decide after Codex's structure review: inline the seven method definitions back into `HAT_EDUCATOR.md` (~300 words, recommended — hats are already an optional load, so the base boot chain is untouched), or make the playbook load mandatory and add the pointer to all five subject hats. Keep the four SKILL procedures in the playbook either way; those are genuinely situational. | OPEN — diagnosed, fix deferred pending Codex structure review |
| 92 | **Claude's OS-level sandbox does not run on native Windows, so no OS layer constrains a spawned shell process.** Tool-level `Edit`/`Write` denies govern tool calls, not child processes started by `Bash`/`PowerShell`. Demonstrated 2026-08-10: a Claude-authored PowerShell script rewrote 2,713 files and its glob excluded neither `88-JOURNAL` nor `raw/`. Recovered from the `pre-rename-2026-08-10` tag, Google Drive, and the recycled `.tree`; Chris confirmed the journal undamaged. **Boundary chosen: hybrid.** Windows Claude Code stays default for ordinary sessions; WSL2 is used for bulk or scripted work — migrations, mass rewrites, renames. **Present state:** Ubuntu on WSL2 installed, Node v24.19.0 via nvm in user space, Claude Code 2.1.227 resolving on `PATH` in a login+interactive shell. Windows and WSL git report identical state (0 modified, same HEAD). **ACCEPTANCE TEST RUN 2026-08-11 FROM A WSL-LAUNCHED SESSION — FAILED.** The deny does not hold. Probes ran from a script file so paths resolved at runtime and never appeared in the command string, reproducing the shape of the August 10 incident (a glob, not a literal path). Results: a child process **wrote successfully** to `03-WIKIS\PYTHON\raw\.sandbox-probe-2026-08-11.tmp` (rc=0), to the vault root, and to `$HOME` outside the workspace; it also **read the `88-JOURNAL` directory** (rc=0) despite `sandbox.filesystem.denyRead`. `bwrap` is installed and no sandbox marker appears in the child environment, so the probes ran unsandboxed. What *did* hold is the **project** permission layer: any `Bash` command naming `88-JOURNAL` literally was refused before execution — which is exactly the control the August 10 glob evaded. Second defect found by the same run: WSL has its own settings home, and `~/.claude/settings.json` there is a 41-byte theme/model file. The user-settings policy template is **not deployed in WSL**, so `validate_boot_chain.py` returns FAIL on 4 findings and `root_health.py` returns **BLOCKER** — the 11 user-level denies are absent, and `disableBypassPermissionsMode`/`disableAutoMode` are unset, meaning bypass and auto modes are *available* in WSL though locked on Windows. **Net: the environment chosen as the safer one for bulk work is currently the less-governed one.** WSL2 remains an unverified control; do not treat it as a safety boundary for scripted work until this closes. Setup detail: `Session_Logs\DAILY_2026-08-10.md`; test detail: `DAILY_2026-08-11.md`. | August 6; decided and implemented August 10; acceptance test failed August 11 | **HIGH** (raised from MEDIUM 2026-08-11 — the assumed control is absent in both environments) | **(1) DONE 2026-08-11** — user-settings policy deployed to WSL `~/.claude/settings.json` with Chris's correction: the five vault-path denies use the literal `/mnt/c/Users/chris/.ROOT/...` prefix, because `~/.ROOT` expands to `/home/theinternet/.ROOT` in WSL and would have guarded an empty path. Mode locks now pass, so bypass/auto mode are disabled in WSL. This did **not** clear the BLOCKER: `validate_boot_chain.py` (lines 194-200) hardcodes the `~/.ROOT/...` spelling and also fails on unrecognized deny rules, so the correct absolute paths count as 5 missing + 5 unexpected. FAIL is down from 4 findings to 2, both the same path-form mismatch. **Pending decision:** make `user_required_deny` environment-aware — accept either the `~/.ROOT/...` form or the vault's resolved absolute path — rather than weakening the deployed rules to match a Windows-shaped validator. **(2) ANSWERED 2026-08-11 — yes, and the hybrid's premise survives.** `/mnt/c` is v9fs (9p), not DrvFs, and `bwrap --ro-bind` enforces on it: a write to a read-only bind returns `Read-only file system`, rc=1. Tested in the exact configuration bulk work needs — 10 read-only binds derived from the live tree (`88-JOURNAL`, `CASTLE\raw`, and 8 hub `raw\` dirs), vault otherwise writable. Ordinary vault write succeeded (rc=0); direct write into `raw\` blocked; **and a glob-expanded write — the August 10 shape, the one the permission matcher cannot see — was blocked.** So the missing control is not unachievable, it is simply not what Claude Code's sandbox is doing here. **Next action: build `00-BRAIN\scripts\safe_shell.sh`** — a wrapper that derives the read-only bind list from the live tree (so a new hub is protected the day it appears, matching how `validate_boot_chain.py` already derives its required list) and runs bulk/scripted work inside it; then amend the boundary rule so migrations, mass rewrites, and renames must run through it rather than relying on the environment alone. Needs Chris's approval as new governance tooling. **BOUNDED CHECK DONE 2026-08-11 — the sandbox never applies.** Confinement was inspected on four independent dimensions and all four are negative: process ancestry is `bash ← bash ← claude` with **no `bwrap` ancestor**; `Seccomp: 0` and `NoNewPrivs: 0`; no namespace isolation attributable to the sandbox; and network egress to `example.com` returned **HTTP 200** despite `allowedDomains` listing only two hosts. So `sandbox.enabled: true` in project settings has no observable effect on `Bash` tool calls — filesystem *or* network. Scope of that claim: it covers user-approved `Bash` calls, which is the path all real work takes; the auto-allow path could not be exercised because the allow-list entries invoke `python`, which does not exist in WSL (only `python3`). **Consequence: the wrapper is not one option among several — it is the only route to an OS-level control, since the thing that was supposed to provide one is inert.** Probe files deleted by Chris 2026-08-11. Copy-first adopted the same day as the standing control (`AGENT.md` § File Safety item 12). Keep every existing `Edit` deny. | OPEN — **acceptance test FAILED**, but a verified OS-level control now exists in `bwrap`; WSL user settings deployed and boot chain PASS; awaiting approval to build the wrapper |
---

## CLOSED FLAGS

Closed flags live in the monthly ledger:
`00-BRAIN\Session_Logs\Closed Flags\CLOSED_FLAGS_YYYY-MM.md` (current month:
`CLOSED_FLAGS_2026-08.md`). Pre-ledger history
(June 8 – July 11, 83 rows):
`99-ARCHIVE\ARCHIVED_2026-07-11_SYSTEM_FLAGS_CLOSED_TABLE.md`.

---
*Maintained by: Claude + Chris | Reviewed: every session start (HIGH), weekly (MEDIUM), monthly (LOW)*
*Last updated: August 2, 2026*
